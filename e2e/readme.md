# Legacy E2E Assets

This directory contains legacy end-to-end experiment entrypoints and Python
config scripts from the original research workflow.

Use `tmerge run pipeline ...` with YAML configs for new work.

Keep this directory for:

- migration reference
- recovering historical experiments
- comparing old and new pipeline behavior

Avoid adding new primary workflows here unless the goal is temporary migration
support.
